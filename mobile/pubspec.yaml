name: feed_formulation
description: Dynamic least-cost poultry feed formulation.
publish_to: "none"
version: 0.2.0+1
application_id: com.egide.feed_formulation
application_name: FeedOptima

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  provider: ^6.1.2
  shared_preferences: ^2.3.2
  intl: ^0.19.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
